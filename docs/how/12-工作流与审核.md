# 12 · 工作流与审核(Workflow Stub)

> 对应代码:`src/writer/workflows/{__init__,params,types,write_chapter,review_chapter}.py`
> 设计备忘:[`备忘 06-长任务质量控制与自动回流`](../../技术难点与解决方案备忘/06-长任务质量控制与自动回流.md) + [`备忘 04-LangGraph多阶段编排`](../../技术难点与解决方案备忘/04-LangGraph多阶段编排与子代理隔离.md)

---

## 12.1 设计动机

**问题**:LLM 工具循环擅长**短交互**(读文件 → 写文件 → 回答),但**长任务**(写一章 3000 字、跨章一致性检查)需要**多阶段编排**——

- 阶段 1:基于大纲 + 上一章摘要生成 outline
- 阶段 2:写正文(可能分 5 段,每段都校验连续性)
- 阶段 3:proofread(语法 / 错别字)
- 阶段 4:[history_check](题材=历史 时启用)
- 阶段 5:review_gate(审核是否通过)

**两种编排范式**:

| 范式 | 状态机 | 优势 | 劣势 |
| ---- | ------ | ---- | ---- |
| **手写同步 stub**(本项目当前) | `WORKFLOWS = {"write_chapter": ..., "review_chapter": ...}` | 简单、可控 | 真实长任务还要写 LangGraph |
| LangGraph `StateGraph` | 节点 + 边 + checkpointer | 自动 checkpoint / interrupt / resume | 学习成本高,需要状态 schema |

**当前选择**:手写同步 stub,等 LangGraph 落地时升级 `_DefaultEngineDeps.run_workflow` 为 `WorkflowStarter.start` async generator。`EngineDeps` Protocol 上**预留 `workflow_starter` 字段**(2026-07-03 占位)。

## 12.2 `WorkflowResult` — 工作流输出契约

> 对应代码:`src/writer/workflows/types.py`

```python
@dataclass(frozen=True)
class WorkflowResult:
    status: Literal["completed", "pending", "failed"]
    chunks: list[str] = field(default_factory=list)        # 流式文本块
    artifacts: dict[str, Path] = field(default_factory=dict)  # 产物路径
    metrics: dict[str, Any] = field(default_factory=dict)  # 决策指标


class WorkflowStub(Protocol):
    """工作流签名:接收 EngineContext + EngineDeps,返回 WorkflowResult。"""
    def __call__(self, ctx: EngineContext, deps: EngineDeps) -> WorkflowResult: ...
```

### `status` 语义

| status | Done reason | payload 关键字段 | 含义 |
| ------ | ----------- | ---------------- | ---- |
| `completed` | `workflow_completed` | `artifacts` / `metrics` | 成功跑完 |
| `pending` | `aborted` | `decision="needs_rewrite"` | 需要上游动作(PR3+) |
| `failed` | `aborted` | `error` | 真实失败 |

**`pending` 设计**:让 `review_chapter` 在「需要重写」时返回 `pending` 而非 `failed`,Engine 把 `decision` metric 透传到 payload,REPL 据此展示「需要重写」提示。

## 12.3 `WORKFLOWS` 注册表

> 对应代码:`src/writer/workflows/__init__.py`

```python
def write_chapter_stub(ctx: EngineContext, deps: EngineDeps) -> WorkflowResult:
    """/创作 stub:同步,产出占位文本。"""
    return WorkflowResult(
        status="completed",
        chunks=["[workflow write_chapter stub] 请等待真实 LangGraph 图落地\n"],
        artifacts={},
        metrics={"chapter": _extract_chapter_arg(ctx)},
    )


def review_chapter_stub(ctx: EngineContext, deps: EngineDeps) -> WorkflowResult:
    """/审核 stub:同步,产出占位文本。"""
    return WorkflowResult(
        status="completed",
        chunks=["[workflow review_chapter stub] 请等待真实审核流程落地\n"],
        artifacts={},
        metrics={"target": _extract_review_arg(ctx)},
    )


WORKFLOWS: dict[str, WorkflowStub] = {
    "write_chapter": write_chapter_stub,
    "review_chapter": review_chapter_stub,
}


def run_workflow(name: str, ctx: EngineContext, deps: EngineDeps) -> WorkflowResult:
    """EngineDeps.run_workflow 的辅助函数:查找 WORKFLOWS,执行 stub。"""
    stub = WORKFLOWS.get(name)
    if stub is None:
        raise WorkflowNotFoundError(f"未知工作流 {name!r}; available: {sorted(WORKFLOWS)}")
    return stub(ctx, deps)
```

### `_DefaultEngineDeps.run_workflow`

```python
def run_workflow(self, name: str, ctx: EngineContext) -> WorkflowResult:
    runner = self._workflows.get(name)
    if runner is None:
        available = sorted(self._workflows)
        raise WorkflowNotFoundError(f"未知工作流 {name!r}; available: {available}")
    from writer.workflows import run_workflow as _dispatch
    return _dispatch(name, ctx, self)
```

## 12.4 `WorkflowNotFoundError` — 领域异常

> 对应代码:`src/writer/tools/errors.py`

```python
class WorkflowNotFoundError(ToolError):
    """未知工作流名称。"""
```

**为什么是 `ToolError` 子类**:工作流是引擎可调用的能力之一,workflow 抛错与 tool 抛错走同一条 `except ToolError` 兜底路径,UX 一致。

## 12.5 `write_chapter` — 章节写作(未来 LangGraph 落地点)

> 对应代码:`src/writer/workflows/write_chapter.py`

当前是 stub,但接口已经预留好让未来 LangGraph 图直接接入:

```python
@dataclass(frozen=True)
class WriteChapterParams:
    """章节写作参数。"""
    chapter_index: int
    chapter_title: str
    outline_excerpt: str       # 该章对应的大纲片段
    characters_excerpt: str    # 该章涉及的人物片段
    prior_summary: str         # 上一章摘要
    canon_block: str           # 4 层文件拼装的正典上下文
    target_word_count: int = 3000
    temperature: float = 0.7


def write_chapter_stub(ctx: EngineContext, deps: EngineDeps) -> WorkflowResult:
    """当前 stub 实现:返回 WorkflowResult 占位。"""
    params = _extract_params(ctx, WriteChapterParams)
    return WorkflowResult(
        status="completed",
        chunks=[
            f"[workflow write_chapter stub] 第 {params.chapter_index} 章 {params.chapter_title!r}\n"
            f"  target: {params.target_word_count} 字\n"
            f"  outline: {params.outline_excerpt[:80]}...\n"
            f"  prior: {params.prior_summary[:80]}...\n"
        ],
        artifacts={},
        metrics={"chapter": params.chapter_index, "title": params.chapter_title},
    )
```

### 未来 LangGraph 节点契约(per 备忘 04)

```python
class WriteChapterState(TypedDict):
    """LangGraph state 字典(未来)。"""
    project_root: Path
    chapter_index: int
    chapter_title: str
    canon_block: str
    draft: str                  # 当前草稿
    proofread_passed: bool
    history_check_passed: bool  # 仅历史题材
    review_passed: bool


# 未来图(伪代码)
builder = StateGraph(WriteChapterState)
builder.add_node("outline_chunk", _outline_chunk_node)        # 抽该章大纲
builder.add_node("write", _write_node)                         # LLM 生成正文
builder.add_node("proofread", _proofread_node)                 # 语法检查
builder.add_node("history_check", _history_check_node)         # 历史题材才启用
builder.add_node("review_gate", _review_gate_node)             # 审核

builder.add_edge("outline_chunk", "write")
builder.add_edge("write", "proofread")
builder.add_conditional_edges("proofread", ...)
builder.add_edge("review_gate", END)
```

## 12.6 `review_chapter` — 章节审核

> 对应代码:`src/writer/workflows/review_chapter.py`

```python
@dataclass(frozen=True)
class ReviewChapterParams:
    """审核参数。"""
    chapter_index: int
    target: Literal["大纲", "骨架", "正文"]
    focus: list[str] = field(default_factory=list)  # 审核重点


def review_chapter_stub(ctx: EngineContext, deps: EngineDeps) -> WorkflowResult:
    """当前 stub:返回 needs_rewrite 决策,演示 pending 路径。"""
    params = _extract_params(ctx, ReviewChapterParams)
    # 真实实现:用 LLM 评审 → 产 ReviewVerdict
    return WorkflowResult(
        status="pending",
        chunks=[
            f"[workflow review_chapter stub] {params.target} 第 {params.chapter_index} 章\n"
            f"  decision: needs_rewrite\n"
        ],
        artifacts={},
        metrics={"decision": "needs_rewrite", "chapter": params.chapter_index, "target": params.target},
    )
```

### `ReviewVerdict` (per real-writing-pipeline PR2)

```python
class ReviewVerdict(BaseModel):
    """审核结构化输出。"""
    decision: Literal["pass", "needs_rewrite", "block"]
    issues: list[Issue] = Field(default_factory=list)
    confidence: float = 0.0
```

`_run_workflow` 把 `decision="needs_rewrite"` 映射到 `Done(reason="aborted", payload={"decision": "needs_rewrite"})`。

## 12.7 引擎 `_run_workflow` 分派

> 对应代码:`src/writer/engine/loop.py::_run_workflow`

```python
async def _run_workflow(name, ctx, deps, cfg):
    yield TextChunk(text=f"[engine] 工作流 {name} 启动\n")
    result = deps.run_workflow(name, ctx)
    for chunk in result.chunks:
        yield TextChunk(text=chunk)
    if result.status == "completed":
        yield Done(reason="workflow_completed", payload={"workflow": name, "artifacts": {...}, "metrics": ...})
    elif result.status == "pending":
        decision = str(result.metrics.get("decision", "needs_rewrite"))
        yield Done(reason="aborted", payload={"workflow": name, "decision": decision, "metrics": ...})
    else:  # failed
        yield Done(reason="aborted", payload={"workflow": name, "error": ...})
```

### `Decision` metric 透传

`pending` 状态以 `Done(reason="aborted", payload={"decision": "needs_rewrite"})` 暴露。REPL 渲染时:

```python
if done.reason == "aborted" and "decision" in done.payload:
    console.print(f"[yellow]工作流 {done.payload['workflow']} 决策: {done.payload['decision']}[/yellow]")
```

让用户知道「需要重写」而非「失败」。

## 12.8 `Bug 03` 修复历史:`review_chapter` 不再早返

**之前**:`review_chapter_stub` 早返 `LLMProseClient.generate_chapter(...)`,即使没 LLM 也调,**测试注入的 fake 不被调用**。

**修复**(per commit `ad45896`):

```python
# 之前(早返)
def review_chapter_stub(ctx, deps):
    if deps.review_llm is None:
        return WorkflowResult(status="pending", ...)  # 永远走 fallback
    return _real_review(ctx, deps)

# 之后(让 fallback 路径自然走 API key)
def review_chapter_stub(ctx, deps):
    review_llm = deps.review_llm
    if review_llm is None:
        review_llm = _maybe_build_review_llm(deps)  # 走 settings + api_key
    return _real_review(ctx, deps, llm=review_llm)
```

`production_deps` 把 `review_llm=None` 留给上层决定,stub 内部根据 API key 决定是否构造;测试可以直接传 fake,生产走 settings 路径。

## 12.9 workflow_starter 预留(per 备忘 04 演进)

未来当 LangGraph 接入,`_DefaultEngineDeps.run_workflow` 升级为 `WorkflowStarter.start`:

```python
class WorkflowStarter(Protocol):
    """未来 LangGraph 工作流入口。"""
    def start(
        self, name: str, ctx: EngineContext, *, fresh: bool = True
    ) -> AsyncIterator[Event]: ...
```

### 未来演进路径

```
当前:
    engine._run_workflow → deps.run_workflow(name, ctx) → sync stub → WorkflowResult
未来:
    engine._run_workflow → deps.workflow_starter.start(name, ctx, fresh=True) → async gen of Event
```

`EngineDeps` Protocol **预留** `workflow_starter` 字段(2026-07-03 占位,目前未装配)。当 LangGraph 落地时,只需扩展 Protocol,不影响 engine.loop 边界。

## 12.10 完整数据流:`/创作 第3章 夜奔`

```
LLM IntentRouter 产出 AgentAction(start_workflow, command="/创作", workflow="write_chapter")
   ↓
Engine:
    case "start_workflow":
        async for event in _run_workflow("write_chapter", ctx, deps, cfg):
            yield event
   ↓
_run_workflow:
    yield TextChunk("[engine] 工作流 write_chapter 启动\n")
    result = deps.run_workflow("write_chapter", ctx)
        → write_chapter_stub(ctx, deps):
            params = WriteChapterParams(chapter_index=3, chapter_title="夜奔", ...)
            return WorkflowResult(status="completed", chunks=["[stub]..."], ...)
    for chunk in result.chunks: yield TextChunk(chunk)
    yield Done(reason="workflow_completed", payload={"workflow": "write_chapter", "artifacts": {}, "metrics": {"chapter": 3}})
   ↓
CLI:
    console.print("[done] workflow_completed")
    session.record_turn("/创作 第3章 夜奔", "workflow_completed")
```

---

## 12.11 进一步阅读

- [05-引擎核心](05-引擎核心.md) —— `_run_workflow` 分派
- [备忘 06-长任务质量控制与自动回流](../../技术难点与解决方案备忘/06-长任务质量控制与自动回流.md)
- [备忘 04-LangGraph多阶段编排与子代理隔离](../../技术难点与解决方案备忘/04-LangGraph多阶段编排与子代理隔离.md)